def fetch_sales(order_received , order_delivered):
    print(f"Total number of ordered received are {order_received} and delivered are {order_delivered}")

def filter_valid_orders(valid_orders , fake_orders):
    print(f"Total valid orders were { valid_orders} and rejected orders were {fake_orders}")

def summarize_data(original_order , order_delivered):
    print(f"Order received {original_order} and delivers were {order_delivered}")
